---
title: Rulers
description: Time & Frequency ruler visualization.
---


```astro live
<div class="viewer-panel">
  <div class="layout-grid">
    <div class="grid-corner-top"></div>

    <div class="grid-top-ruler" id="container-top-ruler">
      <canvas id="canvas-top-ruler"></canvas>
    </div>

    <div class="grid-freq-ruler" id="container-freq-ruler">
      <canvas id="canvas-freq-ruler"></canvas>
    </div>

    <div class="grid-spectrogram" id="container-spectrogram">
      <canvas id="canvas-spectrogram"></canvas>
    </div>
  </div>
</div>

<script>
  import { Sonoscope } from '@sonoscope/core';

  const audioUrl = '/audio/marico-sunbird.ogg';

  const scope = await Sonoscope.fromUrl(audioUrl);

  const topRulerCanvas = document.querySelector('#canvas-top-ruler');
  const time = scope.createTimeRuler(topRulerCanvas, { tickPosition: "bottom" });

  const leftRulerCanvas = document.querySelector('#canvas-freq-ruler');
  const freq = scope.createFrequencyRuler(leftRulerCanvas);

  const specCanvas = document.querySelector('#canvas-spectrogram');
  const spectrogram = scope.createSpectrogram(specCanvas);

  scope.attachNavigation(specCanvas, { axis: "both" });
</script>

<style>
  .viewer-panel {
    padding: 1.25rem;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .layout-grid {
    display: grid;
    grid-template-columns: 64px 1fr;
    grid-template-rows: auto auto;
    gap: 0;
    overflow: hidden;
  }

  .grid-corner-top {
    grid-column: 1;
    grid-row: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.65rem;
    color: var(--text-muted);
    font-family: ui-monospace, monospace;
  }

  .grid-top-ruler {
    grid-column: 2;
    grid-row: 1;
    position: relative;
    height: 28px;
  }

  .grid-freq-ruler {
    grid-column: 1;
    grid-row: 2;
    position: relative;
    width: 64px;
    height: 380px;
  }

  .grid-spectrogram {
    grid-column: 2;
    grid-row: 2;
    position: relative;
    width: 100%;
    height: 380px;
  }

  .grid-corner-bottom {
    grid-column: 1;
    grid-row: 3;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.65rem;
    color: var(--text-muted);
    font-family: ui-monospace, monospace;
  }

  canvas {
    display: block;
    width: 100%;
    height: 100%;
  }
</style>
```
