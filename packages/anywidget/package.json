{
	"name": "@sonoscope/anywidget",
	"version": "0.2.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/sonoscope/static",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@sonoscope/core": "^0.2.0",
		"@sonoscope/halftone-spectrogram": "^0.2.0",
		"@sonoscope/terrain-spectrogram": "^0.2.0",
		"@sonoscope/topographic-spectrogram": "^0.2.0"
	},
	"devDependencies": {
		"@anywidget/types": "^0.3.0",
		"esbuild": "^0.28.0",
		"typescript": "^6.0.2"
	}
}
