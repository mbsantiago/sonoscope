{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/widget.ts --minify --format=esm --bundle --outdir=src/sonogram/static",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@anywidget/types": "^0.3.0",
		"esbuild": "^0.28.0",
		"typescript": "^6.0.2"
	}
}
